const { usersRef } = require("./firebase");

module.exports.Register = async (params) => {
    // const query = await usersRef
    //   .where("username", "==", params.username)
    //   .where("password", "==", params.password)
    //   .get();
    // if (query.empty) {
    //   return { token: false };
    // }
    // let result = {};
    // query.forEach((doc) => {
    //   result = { token: doc.id, username: doc.data().username };
    // });
    // return result;
  };
  